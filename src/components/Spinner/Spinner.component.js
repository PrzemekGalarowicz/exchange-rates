import React from 'react';

import { Container, Loader, Overlay } from './Spinner.style';

const Spinner = () => (
  <Container>
    <Loader />
    <Overlay />
  </Container>
);

export default Spinner;
